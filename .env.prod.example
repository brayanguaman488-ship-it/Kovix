# Copiar este archivo como .env.prod y ajustar valores reales de produccion.

# Dominio principal del panel web
WEB_DOMAIN=panel.tu-dominio.com

# Dominio de API (subdominio recomendado)
API_DOMAIN=api.tu-dominio.com

# Credenciales PostgreSQL
POSTGRES_USER=kovix
POSTGRES_PASSWORD=cambia_esto_con_password_fuerte
POSTGRES_DB=kovix_db

# JWT
JWT_SECRET=cambia_esto_con_un_secret_largo_y_aleatorio

# Usuario admin inicial
ADMIN_USERNAME=admin
ADMIN_PASSWORD=cambia_esto
